# logic-lord-bug-search
Object-oriented Java program that simulates Logic’s teleport-based search across a 100×100 grid. It reads random starting spots from an input file, tracks searched locations efficiently, prints the search order, and outputs the total hours including travel and required sleep cycles.
